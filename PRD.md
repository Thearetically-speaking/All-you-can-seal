# PRD — All You Can Seal Web（网页版热封 G-code 处理器）

| | |
|---|---|
| 版本 | v0.1 草稿 |
| 日期 | 2026-07-12 |
| 现状 | 命令行脚本 `allyoucanseal-bambu.py`（Python，本地运行） |
| 目标形态 | 纯静态网页应用，托管于 GitHub Pages |

---

## 1. 背景与目标

现有的 Python 脚本可以把 Bambu Studio 切片产出的 `.gcode.3mf` 改写为 TPU 热封 G-code（改温度、Z 偏移、流量倍率、移动速度）。但它有明显的使用门槛和已知缺陷：

- 需要本地安装 Python，命令行操作，对非技术用户不友好；
- 改写结果不可见——用户无法在打印前直观确认路径和参数是否正确；
- 源打印温度 230°C 被硬编码，切片档位不是 230 时静默失败；
- 不更新 3mf 内的 `Metadata/plate_1.gcode.md5`，改写后校验和失效；
- 只支持单盘（`plate_1.gcode` 写死），多盘文件直接报错；
- 结构标记缺失时"改一半"而不是中止，可能产出危险的半成品文件。

**目标**：做一个零安装、拖拽即用的网页工具，覆盖并修复以上全部问题，让用户从"切片完成"到"拿到可打印的热封 3mf"在浏览器内一站完成，且在导出前能像切片软件一样预览整个热封路径。

**非目标（Out of Scope）**：
- 不做切片功能——用户仍需用 Bambu Studio 产出 `.gcode.3mf`；
- 不做非 Bambu 方言（Prusa/Klipper）的支持（架构上预留扩展点即可）;
- 不做打印机直连（LAN/云端发送）；
- 不做账号系统、云存储——文件全程只存在于用户浏览器内存中。

---

## 2. 用户与使用场景

**目标用户**：软体机器人 / 可穿戴研究者、maker、教育者。会用 Bambu Studio 切片，但不一定会命令行。

**核心使用流程（Happy Path）**：

1. 打开网页（GitHub Pages URL）；
2. 把 Bambu Studio 导出的 `.gcode.3mf` 拖进页面；
3. 网页自动解析：识别机型、检出切片温度、列出盘（plate）；
4. 用户在参数面板确认/调整四个热封参数（默认值即当前脚本常量）；
5. 3D 预览区显示改写后的完整路径，拖动 slider 逐步查看喷嘴行进过程；
6. 安全检查面板全绿（或用户确认警告）后，点击导出；
7. 浏览器下载改写后的 `.gcode.3mf`，用户发送到打印机。

---

## 3. 功能需求

### FR-1 文件导入（拖拽 + 点击选择）

- 支持拖拽（drag & drop）和点击文件选择两种方式；接受 `.gcode.3mf` 和裸 `.gcode`。
- 导入后立即在浏览器内解包（3mf 即 zip），**文件不上传到任何服务器**，页面上明确标注这一点（隐私是卖点）。
- 解析失败（非 zip、找不到 gcode、非 Bambu 结构）给出具体、可操作的错误提示（如"该文件缺少 CONFIG_BLOCK_END 标记，请确认使用 Bambu Studio 切片并选择 Bambu 打印机档位"）。
- 显示文件基本信息：文件名、机型（从 `;===== machine:` 头解析）、盘数、每盘的层数与预计路径长度、切片时的喷嘴温度。

### FR-2 多盘（multi-plate）处理

- 枚举 3mf 内所有 `Metadata/plate_N.gcode`（不再写死 `plate_1`）。
- 单盘文件：自动选中，无额外交互。
- 多盘文件：显示盘列表（含 3mf 自带的 `plate_N.png` 缩略图），用户可：
  - 选择其中一盘或多盘进行改写（未选中的盘原样保留）；
  - 或"全部改写"。
- 导出时对每个被改写的盘同步更新其对应的 `plate_N.gcode.md5`。

### FR-3 参数面板（可调，默认值沿用现脚本）

| 参数 | 默认值 | 允许范围（硬限制） | 说明 |
|---|---|---|---|
| 喷嘴温度 | 260°C | 180–300°C（按机型上限进一步收紧） | 热封温度 |
| Z 偏移 | −0.15 mm | −0.30 – +0.10 mm | 负值 = 压入膜面 |
| 流量倍率 | 120% | 50–200% | 注入 `M221` |
| 移动速度 | 5 mm/s | 1–30 mm/s | 主体所有独立 `G1 F` 行 |

- 每个参数带滑块 + 数字输入双控件，附一句"这个参数干什么用"的说明和调参方向提示（沿用 README 的 troubleshooting 经验，如"封不牢→温度 +5°C 或 Z 再压低"）。
- 超出允许范围直接阻止输入；接近危险边界（如 Z < −0.20）时显示黄色警告但允许继续。
- 提供"恢复默认"按钮。
- 参数记忆：用 localStorage 记住用户上次的参数（可选、可清除）。

### FR-4 G-code 改写引擎（Python 脚本逻辑的移植 + 修复）

将现有脚本的四类改写移植为 TypeScript 模块，并修复已知问题：

1. **喷嘴温度（修复硬编码，见 FR-8）**：改写开机段中等于"源打印温度"的 `M104/M109 S<t>` 行 → 目标温度；同步改配置头 `nozzle_temperature`、`nozzle_temperature_initial_layer`。开机段固定流程温度（170/140/250 等预热、擦嘴温度）不动。
2. **Z 偏移**：改写 `G29.1 Z… ; for Textured PEI Plate` 行。若找不到该行（非 Textured PEI 床型），列入安全检查警告并说明后果，而不是静默跳过。
3. **流量倍率**：在打印主体 `;VT0` 后注入/替换 `M221 S<flow>`；同步改配置头 `filament_flow_ratio`。
4. **移动速度**：主体内独立 `G1 F<n>` 行 → `G1 F<speed×60>`；行内 `F30000` token → 同值替换。
5. **改写标记**：在 gcode 头部注入一行处理标记（如 `; allyoucanseal: processed v<version> params=<json>`），用于防重复处理（FR-6）和溯源。
6. **md5 同步**：改写后重算并更新 3mf 内对应的 `.gcode.md5` 文件（当前脚本的已知缺陷）。
7. 引擎必须是纯函数式的（输入文本+参数 → 输出文本+变更报告），与 UI 解耦，便于单元测试。

### FR-5 3D 路径预览（核心功能）

类似切片软件的预览体验：

- **渲染内容**：解析改写后主体段的 G0/G1/G2/G3 移动，在 3D 场景中绘制路径折线，叠加打印床（按机型尺寸，A1 mini 180×180 / P1S 256×256）。
- **着色**：至少两种模式——
  - 按移动类型：热封线（挤出移动）vs 空驶（travel，非挤出），空驶用半透明细线；
  - 按速度：色带映射 feedrate，用于确认"主体真的全变成 5 mm/s 了"。
- **步进 slider（核心交互）**：
  - 拖动 slider 从第 0 步到最后一步，路径逐段显示，模拟喷嘴行进；
  - 当前位置显示喷嘴标记（小圆锥/点），旁边浮层显示该步的 X/Y/Z、速度、是否挤出、所在层；
  - 支持播放/暂停按钮（自动步进）和步进速度调节；
  - 多层文件支持按层过滤（层 slider 或下拉，类似 Bambu Studio 的垂直层滑条）。
- **相机**：轨道控制（旋转/平移/缩放），提供俯视图快捷按钮（热封本质是 2D 路径，俯视是主要视角）。
- **对比模式（nice-to-have，P2）**：切换查看改写前/改写后的速度着色差异。
- **性能**：典型热封文件主体几千段移动，直接渲染即可；上限设计按 50 万段移动流畅（BufferGeometry + 增量绘制），解析放 Web Worker，不阻塞 UI。

### FR-6 安全检查（导出前门禁）

导出按钮前置一个检查清单面板，逐项显示 ✅/⚠️/❌：

**硬性检查（❌ 阻止导出，除非用户勾选"我了解风险"强制导出）：**
- 三个结构标记（`CONFIG_BLOCK_END` / `MACHINE_START_GCODE_END` / `MACHINE_END_GCODE_START`）齐全——缺任一项则完全不改写，绝不产出半成品；
- 可执行段温度改写数 > 0（配置头计数与可执行行计数**分开显示**，杜绝现脚本"只改了注释也算成功"的误导）；
- `M221` 成功注入（找到 `;VT0`）；
- 改写后主体无残留高速独立 `G1 F` 行（反向断言）；
- 目标温度不超过该机型热端上限。

**警告级检查（⚠️ 提示但可导出）：**
- 检测到"已处理标记"——文件疑似被本工具或旧脚本处理过（防二次叠加）；
- 机型不在已验证列表（A1 mini、P1S）；
- 未找到 Textured PEI 的 `G29.1` 行（Z 偏移未生效，提示实际后果和替代做法）；
- Z 偏移低于 −0.20 mm；
- 检测到异常拼接行（如旧脚本损坏产生的 `M104 S260M140 S30` 类无换行行）。

每项检查带"这是什么、为什么重要"的展开说明。README 中现有的安全警告（垫硅胶垫、通风、不可离人、先空跑）在导出成功页显著展示一次。

### FR-7 导出

- 在浏览器内重打包 3mf：被改写的 `plate_N.gcode` 与对应 `.md5` 更新，其余 zip 条目字节级原样保留（缩略图、model、config 等）。
- 下载文件名：`<原名>-sealed.gcode.3mf`（不覆盖原文件语义，用户手里永远留有原件）。
- 提供"变更报告"视图：每类修改的实际改动数、旧值→新值（真实解析值，不是写死的展示文案）；可下载 before/after 纯文本 gcode 供 diff。
- 裸 `.gcode` 输入则导出改写后的 `.gcode`。

### FR-8 源温度自动识别（去除硬编码 230）

- 从配置头读取 `nozzle_temperature_initial_layer` / `nozzle_temperature` 作为"源打印温度"，用它（而非硬编码 230）匹配开机段的 `M104/M109` 行。
- 两个值不一致时分别按各自的值匹配替换。
- UI 上显示"检测到切片温度：230°C → 将改为 260°C"，用户可见、可核对。
- 极端情况（配置头缺失该键）：回退为让用户手动输入源温度，同时警告。
- 保留排他保护：等于固定流程温度（预热 170、擦嘴 140 等，按机型 start gcode 特征维护一个列表）的行永不改写，防止源温度恰好撞上流程温度时误伤。

---

## 4. 非功能需求

| 类别 | 要求 |
|---|---|
| 部署 | 纯静态站点，GitHub Actions 自动构建部署到 GitHub Pages；无后端、无 API、无遥测 |
| 隐私 | 文件全程在浏览器内存处理，不发出任何网络请求（除加载站点资源本身）；页面明示 |
| 浏览器 | 最新 Chrome / Edge / Firefox / Safari 桌面版；移动端只需只读预览可用，处理流程以桌面为主 |
| 性能 | 50 MB 的 3mf 从拖入到预览就绪 < 5 秒（解析在 Web Worker）；slider 拖动 60 fps |
| 国际化 | 中英双语（UI 文案抽离，右上角切换），默认跟随浏览器语言 |
| 可测试性 | 改写引擎 100% 纯函数 + 单测覆盖；用仓库的真实 3mf 建立回归 fixture（注意：现有 `test1.gcode.3mf` 已被旧脚本处理过且含损坏行，需重新切一个干净的测试文件，损坏文件可留作"已处理检测"的负例） |
| UI 风格 | 现代、简洁：单页三栏式布局（左：文件与参数，中：3D 预览，右：安全检查与导出）；深色模式支持；参考 Bambu Studio 预览页的视觉语言但不抄袭其品牌元素 |

---

## 5. 技术方案建议（供实现参考，非强制）

- **框架**：Vite + React + TypeScript（或 Svelte，团队熟悉为准）；
- **3D**：Three.js（`Line2`/`BufferGeometry` 增量绘制路径；OrbitControls）；
- **Zip**：`fflate`（轻量、支持流式）或 JSZip；md5 用 `spark-md5`；
- **G-code 解析**：自研轻量解析器（只需 G0/G1/G2/G3/M104/M109/M221 与注释标记，无需完整方言支持），运行在 Web Worker；
- **状态**：文件 → 解析结果 → 参数 → 改写结果 → 检查结果，单向数据流；参数变更即时重算改写与检查（改写引擎足够快，无需手动"应用"按钮）；
- **部署**：`gh-pages` 分支或 Actions `deploy-pages`，PR 预览可用 Actions artifact。

---

## 6. 里程碑建议

| 阶段 | 内容 | 验收 |
|---|---|---|
| M1 引擎移植 | TS 改写引擎 + 单测 + FR-8 动态温度 + md5 修复 | 对干净 fixture 的输出与预期逐行一致；旧脚本已知 bug 全部有对应回归测试 |
| M2 基础流程 | 拖拽导入 → 参数面板 → 安全检查 → 导出（无 3D） | 全流程可在 GitHub Pages 上跑通，含多盘 |
| M3 3D 预览 | 路径渲染 + 步进 slider + 着色模式 | 拖 slider 可复现喷嘴全程；速度着色可肉眼验证 5 mm/s |
| M4 打磨 | 双语、深色模式、移动端只读、对比模式 | — |

---

## 7. 开放问题

1. 多盘"全部改写"时参数是否允许每盘不同？（v1 建议：全局统一参数，简化 UI）
多盘的适合只改写第一盘
2. 是否需要在预览中估算总热封耗时（Σ 路径长度 ÷ 速度）并显示？（成本低、价值高，建议做）
可以
3. 机型热端温度上限表放哪维护？（建议：仓库内 JSON，社区可 PR 补充新机型）
json可以
4. 是否保留 Python 脚本作为 CLI 备选并与 TS 引擎共享测试 fixture？（建议保留，README 分别说明）
可以
